# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.next
dist
dist-ssr
*.local

# Stale copy of the project extracted from singapore_food2.0.zip. Kept out of
# git (and out of lint/typecheck — see eslint.config.js and tsconfig.json) so it
# does not report duplicate errors against files that are never built.
singapare_food2.0/

# Editor directories and files
.claude
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# TypeScript build info
tsconfig.tsbuildinfo

git-*.ps1

# Local environment files — keep secrets out of git. Each developer maintains
# their own .env.local (see auto_post.py / the chatbot for the variables read).
.env
.env.*

# Python bytecode
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/

python/logs/

